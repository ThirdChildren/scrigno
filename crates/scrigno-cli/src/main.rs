//! `scrigno --data-dir <dir> <command>` — one data dir = one "device".
//! Mirrors the `scrigno-client::Vault` API; see `docs/ARCHITECTURE.md` §2.

fn main() {
    println!("scrigno-cli: not yet implemented (M3)");
}
