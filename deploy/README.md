# Deploy (milestone M6)

Filled in by the `infra-deploy` agent when M6 starts. Until then the only supported way to run
Scrigno is locally: see `CLAUDE.md` → *Local development workflow* and `just`.

Planned contents: step-by-step for `/opt/scrigno` on the `debian-docker` VM, nginx + certbot,
AdGuard DNS rewrite, `backup.sh` / `restore.sh` with cron line, restore drill checklist.
